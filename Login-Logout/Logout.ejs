<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Logout Successful</title>
    <style>
        /* Logout */
        .logout-message {
          text-align: center;
          font-size: 4em; 
          color: #181818; 
          font-family: 'Montserrat', sans-serif;
          font-weight: 1400; 
          margin: auto; 
        }

        body {
          display: flex;
          justify-content: center;
          align-items: center;
          min-height: 100vh;
          margin: 0; 
        }

        .logout-header {
          text-align: center;
        }

        .checkmark {
          width: 100px; 
          height: 100px; 
          display: block; 
          margin: 0 auto 20px; 
        }

        .checkmark__circle {
          stroke-dasharray: 160px; 
          stroke-dashoffset: 78px;
          animation: stroke .8s ease-in-out forwards;
        }

        .checkmark__check {
          stroke-dasharray: 156px, 156px; 
          stroke-dashoffset: 160px;
          animation: stroke 0.8s ease-in-out forwards; 
        }          

        .logout-message {
          font-size: 2.5em; 
          margin-top: 0; 
        }

        @keyframes stroke {
          100% {
            stroke-dashoffset: 0;
          }
        }
    </style>
</head>
<body>
    <div class="logout-header">
        <svg class="checkmark" xmlns="http://www.w3.org/2000/svg" viewBox="0 0 52 52">
            <circle class="checkmark__circle" cx="26" cy="26" r="25" fill="none" stroke="#28a745" stroke-width="2"/>
            <path class="checkmark__check" fill="none" stroke="#28a745" stroke-width="2" d="M14,26 L22,34 L38,18"/>
        </svg>
        <h1 class="logout-message">Logout successfully!</h1>
    </div>
</body>
</html>